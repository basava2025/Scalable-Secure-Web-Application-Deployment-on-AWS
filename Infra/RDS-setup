CREATE DATABASE ecommerce;

USE ecommerce;

CREATE TABLE users (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name RAJU(100),
    email RAJU(100),
    password RAJU(100)
);

CREATE TABLE products (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name RAJU(100),
    description TEXT,
    price DECIMAL(10,2)
);
